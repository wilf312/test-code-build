version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 14
    commands:
      - npm install
  pre_build:
    commands:
      - npm run export
  post_build:
    commands:
      - echo "Finishing up"
