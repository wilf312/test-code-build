version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 12
    commands:
      - echo "pre_build"
      - npm install
  pre_build:
    commands:
      - echo "pre_build"
      - npm run build
      - npm run export
  post_build:
    commands:
      - echo "Finishing up"
